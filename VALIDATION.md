# Validation record — Bolt Yard 0.2

The soft-body update is being built and verified. This file will be updated with the actual successful build, test counts, artifact checksum and render findings before delivery.

The previous 0.1 construction prototype passed 31 engine checks and Android signature verification. Those results do not establish the correctness of the new solver; the build retains those regression checks and adds standalone soft-body tests plus native-extension/scene integration checks.

No physical Android device has been accessed. Installation, simultaneous touch input, screen cutouts, sustained FPS, battery use, temperature and background/resume still need target-phone testing. Native x86_64 solver timing and Mesa software screenshots are not Android performance benchmarks.
